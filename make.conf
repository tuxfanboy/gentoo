# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-march=native -O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
MAKEOPTS="-j8 -l8"
PORTAGE_NICENESS="20"
EMERGE_DEFAULT_OPTS="--jobs=8 --load-average=8"
ACCEPT_LICENSE="*"
ACCEPT_KEYWORDS="~amd64"
USE="X alsa pulseaudio -ieee1394 -3dfx -a52 -accessibility -afs -audit -altivec -aqua -appindicator -ayatana -berkdb -bidi -bluetooth -branding -calendar -cdda -cddb -cdinstall -cdr -cgi -cjk -clamav -connman -consolekit -coreaudio -cracklib -css -cups -dbus -dc1394 -debug -djvu -doc -dv -dvb -dvd -dvdr -esd -emacs -emboss -fortran -freewnn -gif -gimp -gnome -gnome-keyring -gps -gtk -gtk-doc -handbook -hddtemp -ibm -ios -ipod -jack -joystick -kde -kerberos -latex -ldap -libnotify -motif -nas -neon -networkmanager -nls -nntp -plasma -policykit -quicktime -rss -scanner -semantic-desktop -smartcard -spell -startup-notification -systemd -telemetry -upower -vcd -vhosts -vnc -wayland -wifi -wxwidgets -xemacs -xscreensaver -zsh-completion"

# USE="-geoip -geolocation -qt5"
VIDEO_CARDS="nvidia"
INPUT_DEVICES="libinput"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C
GENTOO_MIRRORS="https://gentoo.osuosl.org/ https://mirrors.rit.edu/gentoo/"
GRUB_PLATFORMS="efi-64"
